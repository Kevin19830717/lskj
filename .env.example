# ============================================
# 环境变量配置（复制为 .env 使用）
# ============================================

# PostgreSQL 数据库
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=321738392
DB_NAME=smart_scale
DB_SSLMODE=disable

# Go 后端
SERVER_PORT=8080
GIN_MODE=debug

# JWT 认证
JWT_SECRET=smart-diet-jwt-secret-key-change-me-in-production
JWT_EXPIRE_HOURS=24

# 阿里云百炼 API
DASHSCOPE_API_KEY=sk-07c88e8d2fe8470c934c19682c403467

# 文件上传
UPLOAD_PATH=./uploads
UPLOAD_MAX_SIZE_MB=10

# RAG 服务 (Python)
RAG_SERVER_PORT=8001
SIMILARITY_THRESHOLD=0.3
TOP_K_DEFAULT=5
TEXT_MODEL=qwen-plus
VL_MODEL=qwen-vl-flash
EMBEDDING_MODEL=text-embedding-v2
